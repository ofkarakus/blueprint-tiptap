import styled from 'styled-components';

export const Wrapper = styled.button`
    border: 0;
    width: 30px;
    height: 30px;
    padding: 0;
    font-size: 10px;
    background-color: transparent;
    color: rgba(0, 0, 0, 0.85);
    transition: color 0.25s, transform 0.05s;
    display: flex;
    align-items: center;
    justify-content: center;
`;
