import styled from 'styled-components';
import { ImageUpload } from 'icons';

export const Input = styled.input`
    display: none;
`;

export const ImgUpload = styled(ImageUpload)`
    cursor: pointer;
    width: 50px;
    height: 50px;
`;
