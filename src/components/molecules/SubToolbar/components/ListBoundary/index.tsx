import * as e from '../../styles';
import LabelSizeCoords from '../LabelSizeCoords';

const ListBoundary = () => {
    return (
        <e.Table>
            <LabelSizeCoords />
        </e.Table>
    );
};

export default ListBoundary;
